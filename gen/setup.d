/*
   read mpi.h (by #include-ing it in a test file then pre-processesing)
   identify whether ompi or mpich
   read missing info from mpi.h
   read default DEFINEs from mpicc -show output
   dump missing info in to either mpi.h.ompi.in or mpi.h.mpich.in
*/
